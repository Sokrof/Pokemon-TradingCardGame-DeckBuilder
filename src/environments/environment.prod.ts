// Definición de la constante 'environment' que contiene la configuración del entorno de producción
export const environment = {
    // Indica si la aplicación está en modo producción (true) o desarrollo (false)
    production: true,
    // Define la URL base de la API a la que se realizarán las solicitudes
    apiUrl: 'https://api.pokemontcg.io/v2'
};
